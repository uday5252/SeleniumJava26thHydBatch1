Create a class named FoodItem with the following data members:

 `calories` – represents base calories of the food item
 `price` – represents price value treated as calorie impact

 🔹 Method to Overload

Overload a method named `finalCalories()` in three different ways:

---

 1️⃣ `finalCalories()` — without parameters

This method should return the sum of:

```
calories + price
```

(Assume price adds extra calorie impact)

---

 2️⃣ `finalCalories(int extraCalories)` — with one integer parameter

This method should return:

```
calories + price + extraCalories
```

(Extra calories added from toppings)

---

 3️⃣ `finalCalories(String extraCalories)` — with one String parameter

 Convert the string value into an integer
 Return:

```
calories + price + extraCalories
```

---
NOTE: 
Method Used for Conversion
Integer.parseInt(String)

📌 Explanation (Simple)

extraCalories is a String

To perform addition, it must be converted to int

Integer.parseInt() converts a numeric String into an integer

 🧪 Test Cases

 Test Case 1

Input:

 calories = 200
 price = 50
 Method called: `finalCalories()`

Expected Output:

```
250
```

---

 Test Case 2

Input:

 calories = 200
 price = 50
 extraCalories = 30
 Method called: `finalCalories(30)`

Expected Output:

```
280
```

---

 Test Case 3

Input:

 calories = 200
 price = 50
 extraCalories = "40"
 Method called: `finalCalories("40")`

Expected Output:

```
290
```

